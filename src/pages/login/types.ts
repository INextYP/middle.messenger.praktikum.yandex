import { Button, Input } from '../../components'

export interface LoginPageProps {}

export interface LoginPageKeys {
    form: HTMLFormElement
    login: Input
    password: Input
    loginButton: Button
}
